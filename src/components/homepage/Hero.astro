---
import * as Intro from "src/intro.md";
import Links from "../Links.astro";
---

<section>
  <div class="container">
    <h1>Davis Haupt <Links color="white" align="left" /></h1>

    <div class="bio">
      <Intro.Content />
    </div>
  </div>
</section>

<style lang="scss">
  @import "/src/variables.scss";

  section {
    @include sans-serif;

    background: $accent-color;
    display: flex;
    flex-direction: column;
    border-width: 0 0 12px;
    border-color: $accent-color-hover;
    border-style: solid;
  }
  .container {
    margin: 1.5rem auto 5rem auto;
    @media #{$media-size-phone} {
      margin: 0rem 1rem 3rem 1rem;
    }
    max-width: 600px;
  }
  h1 {
    display: flex;
    justify-content: space-between;
    color: white;

    font-size: 3rem;

    @media #{$media-size-phone} {
      flex-direction: column;
      // margin-bottom: 0;
      margin: 0.5rem 0;
    }
  }
  .bio {
    line-height: 1.75rem;
    font-size: 14pt;
    margin: auto;
    color: white;
    max-width: 100ch;

    :global(a) {
      color: white;
      text-decoration: underline;
      @media print {
        text-decoration: underline;
      }
    }
    :global(p) {
      margin: 0;
    }
  }
</style>
