---
import type { MarkdownInstance } from "astro";

type Props = {
  title: string;
  subtitle?: string;
  accent?: boolean;
};

const { title, subtitle, accent = true } = Astro.props;
---

<section id="intro" class={accent ? "accent" : ""}>
  <h1>{title}</h1>
  {subtitle && <h2>{subtitle}</h2>}
  <slot />
</section>

<style>
  #intro {
    width: 100%;
    text-align: left;

    @media (max-width: 684px) {
      margin: 0;
    }

    :global(h1),
    :global(h2) {
      font-family: var(--font-sans-serif);
      width: 100%;
    }

    :global(h1) {
      margin-bottom: 0;
    }

    :global(h2) {
      font-weight: 400;
      font-size: 1rem;
      margin-top: 0.5rem;
      margin-bottom: 1rem;
    }

    &.accent::after {
      border-bottom-color: var(--accent-color);
      content: "";
      display: block;
      width: 5em;
      border-bottom: 0.125em solid var(--accent-color);
      margin: 2em 0;
      @media (max-width: 684px) {
        margin: 1.5em 0;
      }
    }
  }
</style>
